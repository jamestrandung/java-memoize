Configuration:
  status: warn
  appenders:
    console:
      name: Console
      target: SYSTEM_OUT
      patternLayout:
        pattern: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-40.40c{1.} %-5level : %msg%n"
  loggers:
    root:
      level: info
      appenderRef:
        - ref: Console